
# Local recovery archives; do not commit
backups/
tests/model-validation/results/*
!tests/model-validation/results/.gitkeep

# Allow documentation templates in model validation results.
!tests/model-validation/results/*.md

# ADP generated model-validation summaries
# Timestamped summary outputs are local runtime artifacts unless promoted to a formal report.
tests/model-validation/results/[0-9]*-summary.md

# ADP v2.4 active isolated-validation evidence is local restricted runtime evidence.
artifacts/Evidence/ADP-v2.4-Isolated-Validation-Pre-Runtime/
