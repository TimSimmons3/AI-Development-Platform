You are being evaluated as a local Ollama model inside the AI Development Platform, known as ADP.

Return JSON only.

Do not include markdown.
Do not include explanatory text before or after the JSON.
Do not include comments.

Context:

- Project: AI Development Platform
- Short name: ADP
- Workflow: Plan, Implement, Validate, Document, Snapshot, Release
- Local execution: Ollama
- Local interface: Open WebUI
- Open WebUI posture: localhost-only
- Approved models: llama3.2:1b, llama3.2:3b
- Human review required: true

Task:

Return one JSON object with exactly these keys:

project
short_name
workflow_steps
local_execution
local_interface
security_constraints
approved_models
human_review_required
not_audit_ready_without_review

Rules:

- workflow_steps must be an array.
- security_constraints must be an array.
- approved_models must be an array.
- human_review_required must be boolean true.
- not_audit_ready_without_review must be boolean true.
