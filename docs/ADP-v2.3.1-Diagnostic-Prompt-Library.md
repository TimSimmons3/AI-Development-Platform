# ADP v2.3.1 Diagnostic Prompt Library

## 1. Document Control

- Project: AI Development Platform
- Release: ADP v2.3.1
- Status: Approved for controlled repository promotion
- Repository baseline represented: `253e9dd`
- Planned canonical path:
  - `docs/ADP-v2.3.1-Diagnostic-Prompt-Library.md`
- Gate C tests:
  - `V231-R01`
  - `V231-S01`
- Approved frozen prompt:
  - `V231-R01-P1-v2`
- Model:
  - `llama3.2:3b`

## 2. Superseded Candidate Prompt

Candidate prompt `V231-R01-P1-v1` is not approved for execution.

It combined direct retrieval, model-generated source filename, and exact two-line formatting. That design would make a formatting or filename-echo failure indistinguishable from a retrieval failure.

The candidate is retained only as review history.

## 3. Approved Frozen Prompt

Prompt ID:

- `V231-R01-P1-v2`

Prompt SHA-256 definition:

- ASCII bytes exactly as displayed in the code block
- Unix LF between prompt paragraphs
- No terminal newline after the final period

Prompt SHA-256:

- `0c8ea2c72e6b6e3a002261e95308a3a2722a675d730cc2bd3c14eebe964befce`

```text
Use only the attached Knowledge source.

Copy the complete release-objective sentence for platform identifier NOVA-231-DELTA.

If the source does not contain that sentence, return NOT FOUND.

Do not add factual information that is not in the source.
```

## 4. Expected Target Fact

```text
The release objective for NOVA-231-DELTA is to verify that one exact synthetic fact can be retrieved from one attached source without adding outside information.
```

## 5. Scoring Boundary

`V231-R01-P1-v2` is scored for direct retrieval and grounding, not exact output formatting.

`V231-R01` requires:

- The complete target sentence verbatim
- No contradictory or unsupported factual claim
- No automation, reminder, scheduling, or iCalendar output

`V231-S01` is scored from Open WebUI displayed-source evidence generated by the same run. The model is not required to reproduce the source filename.

The following are observations only during Gate C:

- Preface text
- Additional source-supported text
- Line count
- Labels
- Punctuation outside the target sentence

Exact structure and strict no-extra-text behavior remain assigned to `V231-F01` after Gate C passes.

## 6. Prompt Controls

- The prompt is frozen for the three-run Gate C block.
- No wording change is permitted during the block.
- Each counted run uses a fresh chat.
- The intended Knowledge attachment is proven before submission.
- Only `llama3.2:3b` is authorized.
- No tool, web, function, terminal, or automation feature may be enabled in the chat.
- The prompt is submitted exactly once per fresh chat.
- No correction, follow-up, or retry occurs in the same chat.
- A new prompt-truncation warning fails the run and stops the block.
- Incomplete required evidence makes the attempt INCONCLUSIVE.
- No external approval is required between successful runs in the same approved runtime packet.

## 7. Input-Size Indicator

- Approved source: 474 ASCII bytes and 61 whitespace-delimited words
- Approved frozen prompt: 249 ASCII bytes and 37 whitespace-delimited words
- Approved frozen prompt SHA-256: `0c8ea2c72e6b6e3a002261e95308a3a2722a675d730cc2bd3c14eebe964befce`
- Source plus prompt: 723 ASCII bytes and 98 whitespace-delimited words
- Observed Ollama applied input limit from Gate B: 2050 tokens

These byte and word counts are planning indicators only. They exclude Open WebUI system text, retrieval wrappers, source metadata, model-template content, and tokenizer behavior.

No claim is made that the final assembled runtime request cannot truncate. The bounded Ollama log check is controlling evidence.

## 8. Hold Point

This prompt is approved for controlled repository promotion only.

It is not authorized for execution until all approved Gate C artifacts are committed and pushed, Git synchronization and cleanliness are confirmed, and the final pre-runtime gate passes.
